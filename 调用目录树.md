# FixAgent 调用目录树

## 整体架构

**混合架构**：
- **Orchestrator（调度中枢）** — 意图识别 + 模式路由
- **ReAct 循环** — 子 Agent 内部自主决策工具调用（Thought → Action → Observation）
- **Function Calling** — MemoryAgent 单次调用 + 向量冲突检测

```
用户输入 → Orchestrator（意图识别 + 路由分发）
  ├── CHAT → 直接 LLM 对话
  ├── RETRIEVAL → RetrievalAgent (ReAct) → 检索→评估→追问→返回
  ├── DIAGNOSIS → DiagnosisAgent (ReAct) → 检索→图谱查询→推理→诊断
  ├── GUIDANCE → GuidanceAgent (ReAct) → 检索流程→生成步骤→安全校验
  └── FULL → 串行: Retrieval → Diagnosis → Guidance → 汇总
```

## 模块依赖

```
api/main.py (FastAPI 入口)
│
├── agents/orchestrator_agent.py (调度中枢) ★已实现
│   │
│   ├── agents/intention/ (意图识别) ★已实现
│   │   ├── recognizer.py (LLM 识别，qwen-turbo)
│   │   ├── prompts.py (提示词模板)
│   │   └── fallback.py (关键词兜底)
│   │
│   ├── chains/orchestrator.py (意图→模式映射) ★已实现
│   │
│   ├── CHAT → BaseAgent._build_messages() + LLMService.chat()
│   │
│   ├── RETRIEVAL → RetrievalAgent.run_with_react() ★已实现
│   │   └── tools/knowledge_retrieval_tool.py ★已实现
│   │       └── services/vector_service.py ★已实现
│   │           └── embeddings/text_embedding.py ★已实现
│   │
│   ├── DIAGNOSIS → DiagnosisAgent.run_with_react() ★已实现
│   │   ├── tools/knowledge_retrieval_tool.py ★已实现
│   │   └── tools/graph_query_tool.py ★已实现
│   │       └── services/graph_service.py ★已实现
│   │
│   ├── GUIDANCE → GuidanceAgent.run_with_react() ★已实现
│   │   └── tools/knowledge_retrieval_tool.py ★已实现
│   │
│   └── FULL → _execute_full_pipeline() 串行 ★已实现
│
├── agents/memory_agent.py (记忆整理) ★已实现
│   ├── tools/fact_retrieval_tool.py ★已实现
│   │   └── services/vector_service.py
│   └── services/llm_service.py (function calling 模式)
│
├── services/ ★已实现
│   ├── llm_service.py (阿里云百炼 LLM + chat_with_tools + ReAct trace)
│   ├── vector_service.py (Redis 向量库，FT.SEARCH KNN)
│   └── graph_service.py (Neo4j 图数据库，Cypher 查询)
│
├── embeddings/ ★已实现
│   ├── text_embedding.py (文本 → 1024维向量，text-embedding-v4)
│   ├── image_embedding.py (图片 → 向量，TODO)
│   └── multimodal_embedding.py (图文混合向量，TODO)
│
├── tools/ ★已实现
│   ├── base_tool.py (BaseTool + ToolResult + to_openai_schema()) ★已实现
│   ├── knowledge_retrieval_tool.py (知识库向量检索) ★已实现
│   ├── fact_retrieval_tool.py (事实向量检索，MemoryAgent专用) ★已实现
│   ├── graph_query_tool.py (Neo4j 图谱查询) ★已实现
│   ├── yolo_tool.py (YOLO 目标检测) TODO
│   ├── sam_tool.py (SAM 图像分割) TODO
│   └── document_tool.py (文档解析) TODO
│
├── chains/ (路由映射层) ★已实现
│   └── orchestrator.py (IntentionType → AgentMode 映射)
│   （原 LangChain LCEL 链文件已删除，由 ReAct Agent 内部自主编排替代）
│
├── schemas/ ★已实现
│   ├── models.py (枚举: AgentMode / IntentionType)
│   ├── request.py (API 请求模型)
│   ├── response.py (API 响应模型)
│   └── memory.py (MemorySummary Pydantic Schema)
│
├── config/settings.py ★已实现 (环境变量)
│
└── test_*.py (测试脚本)
```

## 实现状态总览

| 状态 | 文件 | 说明 |
|------|------|------|
| ★已实现 | `agents/base_agent.py` | Agent 基类，含 run() / run_with_react() + trace 记录 / run_stream() |
| ★已实现 | `agents/orchestrator_agent.py` | 调度中枢，全模式（CHAT/RETRIEVAL/DIAGNOSIS/GUIDANCE/FULL）可用 |
| ★已实现 | `agents/retrieval_agent.py` | ReAct 知识检索 Agent |
| ★已实现 | `agents/diagnosis_agent.py` | ReAct 故障诊断 Agent |
| ★已实现 | `agents/guidance_agent.py` | ReAct 维修指引 Agent |
| ★已实现 | `agents/memory_agent.py` | 记忆整理，function calling + Pydantic 校验 + 向量写入 |
| ★已实现 | `agents/intention/` | 意图识别（LLM 优先 + 关键词兜底） |
| ★已实现 | `tools/base_tool.py` | 工具基类，含 to_openai_schema() + 统一异常处理 |
| ★已实现 | `tools/knowledge_retrieval_tool.py` | 知识库向量检索 |
| ★已实现 | `tools/fact_retrieval_tool.py` | 事实向量检索 |
| ★已实现 | `tools/graph_query_tool.py` | Neo4j 图谱查询 |
| ★已实现 | `services/llm_service.py` | 阿里云百炼 LLM + chat_with_tools() + ReAct trace |
| ★已实现 | `services/vector_service.py` | Redis 向量库（FT.SEARCH KNN） |
| ★已实现 | `services/graph_service.py` | Neo4j 图数据库客户端 |
| ★已实现 | `embeddings/text_embedding.py` | 文本向量化（text-embedding-v4，1024维） |
| ★已实现 | `schemas/memory.py` | MemorySummary Pydantic Schema（新建） |
| TODO | `embeddings/image_embedding.py` | 图片向量化 |
| TODO | `embeddings/multimodal_embedding.py` | 图文混合向量 |
| TODO | `tools/yolo_tool.py` | YOLO 目标检测工具 |
| TODO | `tools/sam_tool.py` | SAM 图像分割工具 |
| TODO | `tools/document_tool.py` | 文档解析工具 |

## API 端点状态

| 端点 | 方法 | 状态 | 说明 |
|------|------|------|------|
| `/ai/chat` | POST | **已实现** | 经 OrchestratorAgent 路由（全模式可用） |
| `/ai/chat/stream` | POST | **已实现** | SSE 流式响应（仅 CHAT 模式） |
| `/ai/memory/consolidate` | POST | **已实现** | MemoryAgent 对话整理 |
| `/ai/retrieval` | POST | **占位** | pass 待实现 |
| `/ai/diagnosis` | POST | **占位** | pass 待实现 |
| `/ai/guidance` | POST | **占位** | pass 待实现 |
| `/ai/pipeline` | POST | **占位** | pass 待实现 |

## 架构关键变化（相比旧 LangChain LCEL 管道）

| 旧架构（管道式） | 新架构（ReAct + Function Calling） |
|----------------|--------------------------------|
| LCEL `RunnableBranch` 固定路由 | Orchestrator 意图识别 + LLM 自主决策 |
| 每 Chain 有固定工具调用顺序 | Agent 内部 ReAct 循环自主决定何时调什么工具 |
| LangChain `@tool` 装饰器 | 自研 `BaseTool` 基类 + `to_openai_schema()` |
| 无 trace/可观测性 | `chat_with_tools()` 返回 `trace[]`，记录每轮推理 |
| 固定输出结构 | `MemoryAgent` 用 Pydantic 校验 + `response_format={"type": "json_object"}` |
| 依赖 LangChain | 零框架依赖，直接调用 DashScope API |

## ReAct Trace 可观测性

```
DiagnosisAgent.run_with_react()
    └── LLMService.chat_with_tools()
            │
            ┌── 循环 (每轮记录) ──────────────────────────┐
            │  iteration: 1                              │
            │  action: "tool_call"                      │
            │  tool_calls[0]: {name: "knowledge_retrieval"│
            │                    arguments: {...},        │
            │                    result_summary: "..."}   │
            │  duration_ms: 1840                         │
            ├─────────────────────────────────────────────┤
            │  iteration: 2                               │
            │  action: "tool_call"                       │
            │  tool_calls[0]: {name: "graph_query..."}   │
            │  duration_ms: 2100                         │
            ├─────────────────────────────────────────────┤
            │  iteration: 3                               │
            │  action: "finish"                          │
            │  content_preview: "故障分析..."           │
            │  duration_ms: 1500                         │
            └─────────────────────────────────────────────┘
            │
            └── AgentOutput.metadata.react_trace = [第1轮, 第2轮, 第3轮]
```

## 异常处理架构

```
BaseTool.run()  ← 模板方法
    ├── try: _execute() → ToolResult(success=True)
    ├── except ToolException: → ToolResult(success=False, error=ToolError)
    └── except Exception: → ToolResult(success=False, code="TOOL_ERROR")

BaseAgent.run()  ← 模板方法
    └── except Exception: → AgentOutput(status="error", error_type, error_detail)

BaseAgent.run_with_react()  ← ReAct 模式
    ├── except RuntimeError: → "推理步骤超出限制" (max_iterations exceeded)
    └── except Exception: → AgentOutput(status="error")

api/main.py 端点
    └── 检测 metadata.status="error" → HTTPException(500) 正确返回给 Java
```

## 日志输出点

| 模块 | 位置 | 级别 | 内容 |
|------|------|------|------|
| `api/main.py` | chat() | INFO | session / mode / msg_len / latency |
| `api/main.py` | memory_consolidate() | INFO | session / msg_count / latency |
| `api/main.py` | memory_consolidate() 错误 | ERROR | agent_error=[type] detail |
| `services/llm_service.py` | chat() | DEBUG | model / stream / msg_count |
| `services/llm_service.py` | chat_with_tools() finish | INFO | iterations / total_duration_ms |
| `agents/memory_agent.py` | JSON 解析失败 | WARNING | raw content 前100字符 |

## 层级总结

| 层级 | 包 | 职责 | 状态 |
|------|---|------|------|
| 入口层 | api/ | HTTP 接收/响应 | 全端点可用 |
| 调度层 | agents/ | 意图识别/ReAct/记忆整理 | Orchestrator + 3子Agent + MemoryAgent |
| 路由层 | chains/ | 意图→模式映射 | orchestrator.py 可用，LCEL 链已删除 |
| 工具层 | tools/ | 原子能力封装 | 基类+检索/图谱已实现，3个 TODO |
| 服务层 | services/ | 外部服务封装 | 全部可用 |
| 向量层 | embeddings/ | 向量生成 | text_embedding 已实现，2个 TODO |
| 配置层 | config/ | 环境配置 | 可用 |
| 模型层 | schemas/ | 数据结构 | 全部可用 |